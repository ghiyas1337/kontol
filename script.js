document.getElementById('showFaceButton').addEventListener('click', function() {
    const faceContainer = document.getElementById('faceContainer');
    faceContainer.classList.toggle('hidden'); // Menampilkan atau menyembunyikan gambar
});
